{
    "name": "client-server-template-with-vite",
    "private": true,
    "scripts": {
        "bootstrap": "yarn && node init.js && lerna clean && yarn install",
        "build": "lerna run build",
        "dev:client": "lerna run dev  --scope=client",
        "dev:server": "lerna run dev --scope=server",
        "dev": "lerna run dev",
        "test": "lerna run test",
        "lint": "eslint",
        "format": "lerna run format",
        "preview": "lerna run preview",
        "setup-db": "ts-node ./packages/server/src/db/setup.ts",
        "lint:quiet": "eslint --quiet ."
    },
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "engines": {
        "node": ">=20"
    },
    "devDependencies": {
        "@evilmartians/lefthook": "^1.3.9",
        "@testing-library/jest-dom": "^6.6.3",
        "lefthook": "^1.11.14",
        "@types/node": "^24.0.7",
        "lerna": "^8.2.2"
    },
    "resolutions": {
        "rimraf": "4.4.1"
    }
}
